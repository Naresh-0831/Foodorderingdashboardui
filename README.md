
  # Food Ordering Dashboard UI

  This is a code bundle for Food Ordering Dashboard UI. The original project is available at https://www.figma.com/design/LxuE0RptrmGZaFbbxi8kbI/Food-Ordering-Dashboard-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  